{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true
  },
  "include": ["src/**/*", "test/**/*"],
  "//exclude": "test-tmp holds fixtures a test run generates and git ignores; typechecking it would make the result depend on whether the suite had run.",
  "exclude": ["test/test-tmp/**"]
}
