{
  "name": "@kumikijs/e2e",
  "version": "0.1.14",
  "private": true,
  "type": "module",
  "description": "Kumiki real-browser verification tier — runs the same scenarios in Chromium (Playwright) to catch CSS layout, real focus, and rendering bugs a headless DOM can't see.",
  "license": "Apache-2.0",
  "bin": {
    "kumiki-e2e": "./src/cli.ts"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "lint": "biome check src tests",
    "test:e2e": "playwright test",
    "playwright:install": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "@kumikijs/compiler": "workspace:*",
    "playwright": "catalog:"
  },
  "devDependencies": {
    "@kumikijs/runtime": "workspace:*",
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
