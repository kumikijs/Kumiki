{
  "name": "@kumikijs/tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cross-cutting tests: every example in examples/ must parse, typecheck, and build.",
  "license": "Apache-2.0",
  "scripts": {
    "test": "vitest run",
    "lint": "biome check ."
  },
  "devDependencies": {
    "@kumikijs/cli": "workspace:*",
    "@kumikijs/compiler": "workspace:*",
    "@kumikijs/runtime": "workspace:*",
    "@types/node": "catalog:",
    "happy-dom": "catalog:",
    "typescript": "catalog:",
    "vitepress": "catalog:",
    "vitest": "catalog:"
  }
}
