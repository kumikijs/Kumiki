{
  "steps": [
    {
      "label": "the live branch carries no handler yet",
      "expect": {
        "noErrors": true,
        "state": { "armed": false, "log": "" },
        "domIncludes": ["log=[] armed=false"]
      }
    },
    {
      "label": "none of the four reaches a reducer, which is the whole point of registering lazily",
      "do": { "focus": "input" },
      "expect": { "noErrors": true, "state": { "log": "" } }
    },
    {
      "label": "including the two the scenario tier could not fire until it had a key action",
      "do": { "key": "input", "value": "x" },
      "expect": { "noErrors": true, "state": { "log": "" } }
    },
    {
      "label": "and the hover half of the same pair",
      "do": { "hover": "input" },
      "expect": { "noErrors": true, "state": { "log": "" } }
    },
    {
      "label": "arm the branch that adds all four to the very same element",
      "do": { "clickText": "arm" },
      "expect": {
        "noErrors": true,
        "state": { "armed": true },
        "domIncludes": ["log=[] armed=true"]
      }
    },
    {
      "label": "the reused element must now reach the reducer it did not carry at create time",
      "do": { "focus": "input" },
      "expect": { "noErrors": true, "state": { "log": "F" }, "domIncludes": ["log=[F]"] }
    },
    {
      "label": "and the second of the four, on the same element and the same slot",
      "do": { "blur": "input" },
      "expect": { "noErrors": true, "state": { "log": "FB" }, "domIncludes": ["log=[FB]"] }
    },
    {
      "label": "keydown lands on the same slot, on the element that was there before the branch armed",
      "do": { "key": "input", "value": "x" },
      "expect": { "noErrors": true, "state": { "log": "FBK" }, "domIncludes": ["log=[FBK]"] }
    },
    {
      "label": "and mouseenter, which completes the four the runtime lifts onto every tile",
      "do": { "hover": "input" },
      "expect": { "noErrors": true, "state": { "log": "FBKM" }, "domIncludes": ["log=[FBKM]"] }
    }
  ]
}
