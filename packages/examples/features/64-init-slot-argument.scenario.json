{
  "steps": [
    {
      "label": "both init entries fired; themeAt is the argument that was passed",
      "expect": {
        "noErrors": true,
        "state": {
          "note": "hello from storage",
          "scope": "kumiki:note",
          "themeAt": "kumiki:theme"
        },
        "domIncludes": ["note=hello from storage", "scope=kumiki:note", "themeAt=kumiki:theme"]
      }
    }
  ],
  "effects": {
    "loadNote": [{ "outcome": "ok", "value": { "_tag": "Some", "_0": "hello from storage" } }],
    "loadTheme": [{ "outcome": "ok", "value": { "_tag": "Some", "_0": "dark" } }]
  }
}
