{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "types": ["node"]
  },
  "include": ["src/**/*"],
  "//exclude": "src/dev/ is browser code served verbatim by the dev plugin — it uses the `__KUMIKI_TARGET__` placeholder which only resolves at runtime, so tsc can't resolve it. Linted by Biome via its own browser-targeted ignore pattern.",
  "exclude": ["src/dev/**"]
}
