{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true
  },
  "include": ["src/**/*", "test/**/*"],
  "//exclude": "client.ts keeps the reason it carries in tsconfig.json: a placeholder import the dev plugin only substitutes at runtime, so tsc can't resolve it. A test run writes fixtures under test-tmp/ and git ignores them; typechecking them would make the result depend on whether the suite had run first.",
  "exclude": ["src/dev/client.ts", "**/test-tmp/**"]
}
