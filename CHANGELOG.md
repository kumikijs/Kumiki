# Changelog

English · [日本語](./CHANGELOG.ja.md)

The format follows [Keep a Changelog](https://keepachangelog.com/) and adopts [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added

v0.6 — the **testing DSL completion** milestone — closes the language surface of the in-language `test` layer ([spec/testing.md](./spec/testing.md) §8) that shipped partially in v0.2 (`episode-test` is split to v0.7 — it first needs the runtime episode logger of runtime.md §10.5).

- **v0.6 M4 (#52) — `kumiki test` runner polish** (`spec/testing.md` §8.7): the runner now reports **per-test timings** on every line (`PASS inc-increments (1ms)`; a property-test adds its case count, `(100 cases, 23ms)`). New **`--coverage`** prints, per reducer / effect / tile, how many the suite exercises and lists the uncovered — a reducer-test/property-test covers its target reducer(s) and the effects they emit, a mocked effect result also covers the `.ok`/`.err` reducers it drives, and a tile-test covers its tile (computed statically by codegen into `globalThis.__kumikiCoverage`). New **`--watch`** re-runs the (filtered) suite on `.kumiki` change (debounced; Ctrl-C exits cleanly).
- **v0.6 M3 (#51) — `property-test`** (`spec/testing.md` §8.3): generative testing of reducer invariants. `property-test for-all={n: T, …} given={…} invariant=<bool expr> (count=N)? (shrink=bool)?` generates `count` (default 100) cases per type — `Int`/`Float`/`Text`/`Bool`/`List`/`Map`/`Set`/`Option`/`Result`, plus records (field-wise) and unions (random variant); a refinement folds into its base generator as a **bound** (`between` → range, `nonempty`/`len-*` → length, `positive` → floor) rather than reject-sampling. The `invariant` may chain `run-reducer(name)` steps — each applies a reducer to the current `{slots}` state with the `given` event and returns the next state — and compares the result (`run-reducer(toggle).run-reducer(toggle).slots.todos == todos`). Generation is **seeded** (default: a hash of the test name) so a failure reproduces exactly; on failure the counterexample is **shrunk** to a minimal value (unless `shrink = false`). The runner reports the case count (`(100 cases)`, §8.7.1). `for-all` type references must resolve and every `run-reducer` target must be a declared reducer (E0102). `28-tests.kumiki` gains `inc-dec-roundtrips`.
- **v0.6 M2 (#50) — effect-result mocks inside `reducer-test`** (`spec/testing.md` §8.5): a multi-step flow — a reducer emits an effect whose result drives another reducer (`loadUser.ok($u, _)`) — could only be tested through the browser-driven scenario runner. `reducer-test` now accepts `given.mocks = {effect: ok(v) | err(e) | delay(ms, ok(v))}`; the runner dispatches the trigger and drives the emit → result → reducer loop **headlessly and synchronously** (no DOM, no real time): a mocked effect is delivered to its `.ok`/`.err` reducer (the mock's `value` as the reducer's first bind) and **consumed** (so it leaves `expect.effects`), while a non-mocked emit is **residual** and asserted via `expect.effects`. `delay(ms, …)` resolves immediately (virtualized time, emit order). A mock key must name a declared effect (**E0104**), and a mocked `err` that no `.err` reducer consumes **fails** the test (the v0.5 #37 no-silent-failure contract) rather than passing silently. `28-tests.kumiki` gains `add-surfaces-persist-error` (mocks `persist` → `err`, drives the `.err` reducer, asserts the status it set).
- **v0.6 M1 (#49) — `reducer-test` `expect` wildcards** (`spec/testing.md` §8.2.2): a `reducer-test` whose result includes a freshly generated id (`TodoId.fresh()`) or other non-deterministic field could not be asserted with a literal `expect`. Two wildcards now fill those holes — `<any-id>` matches any present value (and, as a **map key**, pairs with exactly one otherwise-unmatched entry; zero or more than one fails), and `<slots.X>` matches slot `X`'s post-execution value (e.g. `effects: [persist(<slots.todos>)]`). Matching stays **exact** otherwise — records compare by their full key set, with wildcards blanking out only the unpredictable fields (`createdAt: <any-id>`), so no partial-record matching weakens the existing assertions. A wildcard outside a `reducer-test` `expect` (a body, or a test's `given`) is a compile error (**new E0109 `test-wildcard-misuse`**). `28-tests.kumiki` gains an `addItem` reducer-test exercising both wildcards.

- **v0.5 M1 (#39) — Runtime-truth verification tier in the example guard**: `packages/tests/examples.test.ts` asserted only that `compile()` succeeds and `smoke.test.ts` only that an example mounts/renders ("not empty / no throw"), so a "compiles but is actually broken" example shipped green — exactly how the `03-union-and-match` heading bug, lowered to `_s.show(undefined)`, rendered an empty-but-present heading past both. A new corpus guard (`packages/tests/render-guard.test.ts`) targets the dropped-expression class: a static scan of every example's generated JS for the `_s.show(undefined)` sentinel (a zero-false-positive marker — Kumiki source has no `undefined` literal, distinct from the pervasive benign `undefined` in reducer read-back / selector-less reducers, so no allowlist is needed), plus a jsdom render assertion that no example produces a text node literally equal to `"undefined"`. The scanner is extracted as a unit-tested pure function proven to fire on the `03` shape, so a re-introduced dropped-expression bug fails `pnpm test` instead of shipping green. ([spec/testing.md](./spec/testing.md) §8.10)
- **v0.5 M2 (#37) — No-silent-failure contract for effect errors**: when `localStorage` is unavailable (opaque-origin sandbox preview, private mode) the storage capability returns `err`, but `20-effect-storage` wired only `.ok`, so the error was dropped and the app looked dead. M2 extends the v0.3 live-panic ethos ("a failure must never fail silently") to **effect results**: an `err` outcome with **no matching `.err` reducer** is now surfaced via `console.error` (`[kumiki] effect "<name>" returned an error with no .err reducer: …`), so the verification tiers (`smoke` / `runScenario`, which capture `console.error`) flag it — general across all capabilities, not just storage. The default contract stays **`err` + a surfaced report**; a program opts into handling (or deliberately ignoring) the error by wiring an `.err` reducer (even an empty one), and an in-memory storage fallback is explicitly *not* the silent default. `20-effect-storage` and `27-custom-capability` now model their `.err` branch (a visible `unavailable` / `no provider` status). _(Implementation note: the channel is `console.error`, not a dev-only `console.warn` as the roadmap first sketched — chosen for consistency with the live-panic model and because the verification tiers capture `console.error`, making the failure impossible to ignore.)_ ([spec/stdlib.md](./spec/stdlib.md) §2.5)
- **v0.5 M3 (#36) — Virtual / memory router mode for embedded contexts**: path-based routing read/wrote the ambient document `location` / `history`, so it could not initialise or navigate inside the playground `<iframe srcdoc sandbox>` (no real path → initial match fell to `/404`; opaque origin → `history.pushState` throws `SecurityError`) — or any embedded host (Web Component, embed) that owns the top-level URL. M3 introduces a router abstraction with two sources: `mount(app, el, { router: "memory", initialPath?: "/" })` holds the current path in memory — the initial route resolves from `initialPath` (not `location`) and `navigate` / link clicks / `navigate-back` update it and re-render without touching `history.*`; path params, query, redirects (`->>`) and `/404` behave identically. `router: "history"` stays the default (apps at a real origin are unaffected). The playground opts in (`globalThis.__kumikiMount = { router: "memory" }` before the auto-mounting bundle, so `18-routing` / `23-lifecycle-route-enter` work in preview), the auto-mount bundle reads `globalThis.__kumikiMount`, and `defineKumikiElement(tag, app, { router: "memory" })` forwards it to the Web Component. ([spec/routing.md](./spec/routing.md) §3.3.4)
- **v0.5 M4 (#38) — Deterministic HTTP showcase in the playground**: `19-effect-http` fetched the reserved `api.example.com`, so the showcase always rendered the failure state. The example now requests a relative `/api/quote` (so a host serving the bundle next to a real endpoint hits it directly), and the docs playground registers a deterministic `http.get` **provider** (via `globalThis.__kumikiProviders`) that serves the quote — preview-scoped, using the runtime's existing capability-provider seam, with no `fetch` patching and no sandbox weakening — so the showcase demonstrates the **success** path offline while the `.err` path stays reachable against an unreachable endpoint.

v0.5 — the **embedded-robustness & runtime-truth verification** milestone — is complete (all four items shipped: a Kumiki app that compiles green can no longer be silently wrong or non-functional in an embedded / sandboxed context).

## [0.3.0] - 2026-06-04

v0.3 — the type-soundness & robustness milestone — is complete (both items shipped).

### Fixed

- **M2 (#23) — Receiver type inference for method-shortcut dispatch** (`spec/stdlib.md` §2.2.3, `spec/errors.md` E0108): `recv.method` (the parenthesis-free shortcut) was dispatched **by name only**, so a record field named like a method (`node.head` on `{head, tail}`) was silently shadowed by the method, and an unknown `recv.bogus` compiled to `undefined`. M2 adds the checker's first **type-inference pass** (a name→type environment for slots / `fn` params / tile `in` / `let`, plus `inferType`): `FieldAccess` now dispatches field-vs-shortcut by the receiver's **inferred type**, so a same-named record field is read as a field, and an unknown member on a **known** receiver type is a compile error (**new E0108 `undef-member`**) instead of silent `undefined`. The inference is conservative — an undecidable receiver keeps the prior name-based dispatch (no regression). Also fixes the asymmetry where some no-paren shortcuts (`is-ok` / `is-err` / `values` / `entries` / `lower` / `upper` / `sort` / `ms`) were missing from `KNOWN_METHODS` (so `.m()` tripped E0801 while `.m` worked). New example `examples/features/33-field-vs-method.kumiki`. _Note:_ E0108 is a deliberate tightening — a program that previously compiled `recv.bogus` to `undefined` now fails to compile.
- **M1 (#24) — Clean panic handling on the live path** (`spec/lifecycle.md` §7.2–7.3, `spec/stdlib.md` §2.2): a panic on the **live** path — `panic(message)`, `Result.get-err` on `Ok`, or the polymorphic `.get` on the empty case (`None` / `Err`) — previously escaped the DOM event handler / render as an **uncaught exception**, aborting the dispatch and leaving the DOM stale. Now there is one panic model: a tagged `KumikiPanic` (raised by all of the above), caught around live reducer dispatch so the episode is **rolled back** (no partial slot writes), surfaced via `console.error` (so the `smoke` / scenario tiers still flag it), and routed to the `app.error` reducer with `PanicInfo` as `$event`; the app stays interactive. A render panic with no enclosing `error-boundary` now renders a built-in **top-level fallback** instead of throwing. Two latent bugs are fixed along the way: `panic(message)` was never implemented (it lowered to an undefined function call), and `.get` returned the value unchanged on `None` / `Err` (silent) — opposite to `.get-err` — and did not even unwrap `Ok`; `.get` now unwraps `Some`/`Ok` and panics on `None`/`Err`, consistent with the spec. New example `examples/features/32-panic-boundary.kumiki`.

## [0.2.1] - 2026-06-04

### Fixed

- **Issue #7 — argument-less stdlib methods** (`spec/stdlib.md` §2.2): `head` / `tail` / `last` / `to-list` / `get-err` / `to-option` / `parse-int` / `parse-float` / `abs` / `neg` / `to-float` / `to-int` were unimplemented, so the **parenthesis-free form the spec recommends** (`list.head`) compiled clean but evaluated to `undefined` at runtime (silent wrong result), while the parenthesized form (`list.head()`) was hard-rejected with **E0801**. Both shapes now lower to runtime helpers (`_stdlib.listHead`/`listTail`/`listLast`/`toList`/`getErr`/`toOption`/`parseIntOpt`/`parseFloatOpt`; `Math.abs`/`Math.trunc` for the numerics) and are added to `KNOWN_METHODS`. New example `examples/features/31-argless-methods.kumiki`. Follow-up to #5. _(The method-shortcut-shadows-a-field limitation noted here was resolved in v0.3 M2 / #23 — see [0.3.0].)_

## [0.2.0] - 2026-06-03

The five spec-deferred features (M1–M5) shipped as independent milestones.

### Added

- **v0.2 M5 — `motion` layer**: reusable, scoped animations declared with `motion N = {keyframes:{from,to}, duration?, easing?, iteration?, direction?}` and referenced from any tile's `motion` prop. The keyframe grammar is **closed** (animatable set `opacity` / `translate-x` / `translate-y` / `scale` / `rotate`; closed timing tokens) — no raw-CSS escape hatch. `motion` is a top-level definition modeled on `theme` and is **not** counted among the seven logic layers (ADR-001); its literal-only body makes it structurally pure (no slots/effects). The runtime injects scoped `@keyframes` + classes at mount and honors `prefers-reduced-motion`. New errors **E0401** (unknown keyframe property), **E0402** (invalid timing), **E0403** (malformed keyframes), **E0107** (undefined motion). Composes with `when(...)` and `overlay`. New example `examples/features/30-motion.kumiki` (+ a `@kumikijs/e2e` browser scenario asserting a running animation, which jsdom can't observe; the e2e tier gained an `animating` assertion). With M5, **all five v0.2 milestones (M1–M5) are shipped**. ([spec/style.md](./spec/style.md) §4.9.1)
- **v0.2 M4b — `kumiki fix --auto-patch <test-name>`**: `fix` now repairs from a failing `test`, not only typecheck errors. Two tiers: if the file doesn't compile it reuses `planFixes` (did-you-mean / missing `/404`) so the test can run; if a tile-test / reducer-test fails on a **string leaf** whose actual value is a *unique* source literal, it replaces that literal with the expected value (the §8.7.1 snapshot case). `--apply` writes the patch and re-runs the test, reporting whether it now passes and whether any other test regressed; dry-run (no `--apply`) only proposes. Non-literal divergences (numeric slots, wrong operators, effect-list mismatches) are reported as a diff, never guessed. The runner now also prints the §8.7.1 value arrow (`expected -> actual`) when it can isolate a scalar leaf. ([spec/testing.md](./spec/testing.md) §8.7.2)
- **v0.2 M4a — `test` layer + `kumiki test` runner**: in-language tests, run with `kumiki test [name|prefix*]`. `reducer-test R given={slots,event} expect={slots,effects}` checks a reducer's pure output (or `expect={panic:"…"}`); `tile-test T given={slots} expect=<tile-expr>` structurally compares the rendered tile (props/handlers ignored, per spec §8.4). Output is the spec §8.7.1 PASS/FAIL + `expected`/`actual`/`diff at`; exit non-zero on failure. Tests are excluded from `kumiki build` (codegen `includeTests`). Record literals now accept keyword field names (e.g. `type:` / `in:`). New example `examples/features/28-tests.kumiki`. _Not yet implemented_: `property-test`, `episode-test`, `expect` wildcards, effect-result mocks in reducer-test, `--watch`/`--coverage`. ([spec/testing.md](./spec/testing.md) §8)
- **v0.2 M3 — plugin capability registration**: a project can register custom capabilities in a `kumiki.caps.json` manifest (next to the `.kumiki` file); registered names are then accepted in `app.caps` and their effects are emittable + dispatched at the capability boundary (mockable in scenarios, like standard effects). This also implements the spec's long-standing rule that an **unlisted capability is a compile error** — a new standard-capability set + **E0302 `unknown-capability`** (previously the implementation accepted any cap string, diverging from `spec/stdlib.md §2.5`). The manifest is a declarative capability boundary, **not new syntax** (rationale non-goal preserved). Resolved by the CLI, MCP (`capabilities` arg / co-located manifest), and the test harness. New example `examples/features/27-custom-capability.kumiki` + `kumiki.caps.json`. ([spec/stdlib.md](./spec/stdlib.md) §2.5)
- **v0.2 M2 — `overlay` builtin**: z-axis stacking via `overlay(...children)`. The first child is the base layer (normal flow); each later child is placed absolutely over the container (so the base layout never shifts) — the substrate for modals / toasts / dropdowns / tooltips. The `align` prop positions overlaid children (vertical `top`/`bottom` + horizontal `left`/`right` joined with `-`, e.g. `top-left`; default `center`; unknown → `center`). Composes with `when(...)` for mount/unmount; CSS is self-contained (no global-CSS escape hatch). New example `examples/features/26-overlay.kumiki`. ([spec/style.md](./spec/style.md) §4.4.3)
- **v0.2 M1 — `stop-timer(name)`**: a timer trigger can be named with `timer(d, name=N)`, and a reducer can stop it with the `stop-timer(N)` statement. Timer names share one namespace and must be unique (duplicate → **E0002**); a `stop-timer` to an undeclared name is **E0106**. `stop-timer` is a pure control statement — the reducer returns `stopTimers` and the runtime clears the interval, so reducer purity is preserved. All timers (running or stopped) are cleared on `app` dispose. New example `examples/features/25-stop-timer.kumiki`. ([spec/lifecycle.md](./spec/lifecycle.md) §7.1.5)

## [0.1.0]

Initial baseline (published to npm; untagged in git).

### Added

- pnpm + Turborepo monorepo structure (`@kumikijs/compiler` / `@kumikijs/runtime` / `@kumikijs/cli` / `@kumikijs/mcp`).
- `@kumikijs/mcp`: an MCP server exposing the compiler, AI editing, and spec search as MCP tools.
- **Runtime smoke tests**: `@kumikijs/runtime`'s `smoke()`, CLI `kumiki smoke <file>`, MCP `kumiki_smoke`. Mounts to a headless DOM and operates the UI to detect runtime exceptions, empty rendering, and unhandled rejections that `check`/`build` don't catch. All examples are smoke-verified in CI (`tests/smoke.test.ts`). The 3-layer verification model is in [spec/testing.md](./spec/testing.md) §8.10.
- **Scenario runner and autonomous-loop substrate**: `@kumikijs/runtime`'s `runScenario()`, CLI `kumiki run <file> <scenario.json>`, MCP `kumiki_run_scenario`. Drives the app with an operation sequence + slot state assertions, returning the state, DOM, errors, and emits at every step as a trace. Effects are mocked at the capability boundary and are deterministic. Because state is used as the oracle, it can also detect non-exception bugs such as "select always ends up at the last option". The procedure for the human-free generate → run → observe → fix loop is in `.claude/skills/kumiki-iterate`.
- **Real-browser verification tier `@kumikijs/e2e`** (Chromium / Playwright): runs the same scenario format as jsdom in a real browser, capturing layers that jsdom can't verify such as `focused` (real focus), `visible`/`hidden` (computed visibility). Opt-in (browser binaries are heavy, so it's not included in default CI). Example: `examples/apps/06-expenses/scenario.browser.json`.
- `spec/`: reorganized the normative spec. Added a new error code catalog `spec/errors.md` (E0001..E07xx).
- `examples/`: 23 per-feature minimal examples (`features/`) and 5 apps ordered by size (`apps/`). All have parsing, type checking, and build verified in CI.
- `tests/`: behavior-guarantee tests for all examples.
- `guide/`: getting started, first app, mental model, recipes.
- `.claude/skills/`: `kumiki-author` / `kumiki-debug` / `kumiki-iterate` skills.
- **Static method existence check (E0801)**: when `obj.method(...)` calls a method not implemented in the runtime (a typo, misuse like `Option.to-result`, or an unimplemented spec method), it is detected at the `check` stage. The implementation set is the single source of truth in `@kumikijs/compiler`'s `KNOWN_METHODS` (kept in sync with codegen). It catches `.to-result`-class bugs — previously caught only at the smoke layer — at layer 1.
- **`List.fold` / `Int` / `Float.parse` fixes** (found while demoing the iterate loop): implemented `fold`'s codegen + runtime, and fixed `Int.parse`/`Float.parse` to do numeric conversion (previously they returned strings, breaking sums and the like). Examples: `examples/features/24-fold.kumiki`, `examples/apps/06-expenses/`.

### Changed

- Changed the one-reducer-one-write rule from route-name granularity to **path-shape (lvalue shape) granularity**. `tasks[id].status` and `tasks[id].updatedAt` can now coexist.
- Runtime: guard so that delayed effect results after dispose don't touch the DOM (resolves `NotFoundError` caused by in-flight fetches).
- AST: renamed the fields of `IfStmt` / `IfExpr` / `TileIf` from `then`/`else` → `consequent`/`alternate`.

### Notes

- Pre-1.0 baseline. The language, runtime, and tools may change without notice between minor versions.
