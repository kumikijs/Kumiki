{
  "name": "@kumikijs/docs",
  "version": "0.1.14",
  "private": true,
  "type": "module",
  "description": "Kumiki documentation site (VitePress) — serves the spec, guide, and design notes plus a browser playground with WebMCP tools.",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@kumikijs/compiler": "workspace:*",
    "@kumikijs/runtime": "workspace:*",
    "@kumikijs/syntax": "workspace:*",
    "shiki": "^4.2.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitepress": "catalog:",
    "vitest": "catalog:",
    "vue": "^3.5.35"
  }
}
